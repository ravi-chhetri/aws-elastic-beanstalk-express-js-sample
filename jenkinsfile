pipeline {
    agent {
        docker {
            image 'node:16'
        }
    }
    stages {
        stage('Build') {
            steps {
                // Your build steps here
                sh 'npm install'

            }
        }
        stage('Test') {
            steps {
                // Your test steps here
                // sh 'npm test'
            }
        }
        // Add more stages as needed
    }
}
